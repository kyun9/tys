CREATE TABLE professor ( _id INT PRIMARY KEY AUTO_INCREMENT, name VARCHAR(32) NOT NULL, belong VARCHAR(12) DEFAULT 'FOO', phone VARCHAR(12) );
show tables;
desc professor;
CREATE TABLE user ( id varchar(32) PRIMARY KEY,
pwd VARCHAR(32) NOT NULL,
checkn int(10) default 0,
img  varchar(100)
);
insert into user values ('a','aaa',0, "default");	
insert into user values ('b','bbb',0,"default");
insert into user values ('c','ccc',0,"default");
insert into user values ('d','ddd',0, "default");
drop table user;
truncate table user;
select * from user;
commit;
SELECT checkn FROM USER WHERE id = 'a';